#include "main.h"

/**
 * puts_half - print half of a string
 *
 * @str: input string
 * Return: half of a tsring
*/

void puts_half(char *str)
{
	int i = 0;

	while (str[i])
		i++;
	while (j < i / 2)
	{
		_putchar(str[j]);
		j++;
	}
	_putchar('\n');
}
